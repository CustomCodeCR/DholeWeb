# ALL IN and currency Value deployment

Deployment marker for the Pricing wizard fixes verified on 2026-09-10:

- Pantalla 8 exposes the commercial `ALL IN` control.
- Pricing currency selectors render Config `Value`; technical `Code` remains internal.

This commit intentionally triggers the normal branch deployment pipeline so the already-merged Pricing fixes are included in the deployed bundle.
