name: One-time fix seller request build anchor

on:
  push:
    branches:
      - develop
    paths:
      - .github/workflows/one-time-fix-seller-port-handling-anchor.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
      - name: Fix seller port handling build anchor
        run: python3 .github/scripts/fix_seller_port_handling_anchor.py
      - name: Validate patch
        run: git diff --check
      - name: Commit functional fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/one-time-fix-seller-port-handling-anchor.yml
          git rm .github/scripts/fix_seller_port_handling_anchor.py
          git add build/pricingSellerRateRequestResponsibilities.ts
          git commit -m "fix(pricing): stabilize seller request responsibility build"
          git push origin HEAD:develop
